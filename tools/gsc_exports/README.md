# Google Search Console exports — drop folder

Drop GSC **Performance → Queries** (or Pages) CSV exports here. The weekly
indexing report task runs `rank_tracker.py` on the newest one. No GSC connector
is authorized, so this is the manual data path.
